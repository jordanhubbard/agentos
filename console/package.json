{
  "name": "agentos-console",
  "version": "0.1.0",
  "description": "agentOS console bridge — WebSocket server that streams live PD output to the browser dashboard",
  "type": "module",
  "scripts": {
    "console": "node agentos_console.mjs",
    "start": "node agentos_console.mjs",
    "test": "node --test console/test/api.test.mjs",
    "test:watch": "node --test --watch console/test/api.test.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
